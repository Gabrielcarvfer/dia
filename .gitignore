# We only need to ignore meson build directories
build/
build-release/

# Downloaded meson subprojects (the .wrap files stay tracked)
subprojects/*/

.vscode
*.pyc
build-aux/flatpak/build/
.flatpak-builder
*.swp
